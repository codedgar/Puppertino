---
layout: ../../../layouts/Doc.astro
title: Segmented Controls
description: A linear set of segments, each of which functions as a button. macOS 26 metrics, accent and gray styles, no JavaScript required.
---

import '@puppertino/forms.css';
import '@puppertino/segmented-controls.css';

A segmented control is a linear set of two or more segments, each of which functions as a button. One segment is always selected. Use it to switch between views or filter a collection, not to trigger actions.

## When to Use It

Use a segmented control when people pick one of two to five views that swap in place. Day, Week, Month. List, Grid.

Use tabs when the sections carry their own content areas. Use radios when the choice is part of a form and takes effect on submit. Avoid more than five segments on desktop; labels truncate and targets shrink.

## Anatomy

| Part | Value |
|---|---|
| Track | 24px tall, 6px radius, `--p-fill-secondary` background |
| Segments | Equal width, 54px minimum, 10px horizontal padding |
| Label | 13px, weight 510. Unselected `--p-text-primary`, selected white |
| Selected pill | Full height, no inset, 6px radius, flat accent fill |
| Separators | 1px, `#D9D9D9`, 5px vertical inset, hidden next to the selection |

Every value is exact, not approximated.

## Basic Use

<div style="display: flex; flex-wrap: wrap; gap: 20px; align-items: center; padding: 24px; background: var(--p-bg-secondary); border-radius: 10px; margin: 16px 0;">
  <div class="p-segmented" role="group" aria-label="Calendar view">
    <label class="p-segment"><input type="radio" name="sc-view" checked /><span>Day</span></label>
    <label class="p-segment"><input type="radio" name="sc-view" /><span>Week</span></label>
    <label class="p-segment"><input type="radio" name="sc-view" /><span>Month</span></label>
  </div>
  <div class="p-segmented" role="group" aria-label="Sort order">
    <label class="p-segment"><input type="radio" name="sc-sort" checked /><span>Name</span></label>
    <label class="p-segment"><input type="radio" name="sc-sort" /><span>Kind</span></label>
    <label class="p-segment"><input type="radio" name="sc-sort" /><span>Date</span></label>
    <label class="p-segment"><input type="radio" name="sc-sort" /><span>Size</span></label>
    <label class="p-segment"><input type="radio" name="sc-sort" /><span>Tags</span></label>
  </div>
</div>

```html
<div class="p-segmented" role="group" aria-label="Calendar view">
  <label class="p-segment"><input type="radio" name="view" checked /><span>Day</span></label>
  <label class="p-segment"><input type="radio" name="view" /><span>Week</span></label>
  <label class="p-segment"><input type="radio" name="view" /><span>Month</span></label>
</div>
```

It is a radio group wearing a control. Native inputs carry the state, so keyboard navigation, form submission, and screen reader semantics come free. No JavaScript.

Separators sit between segments and disappear next to the selection, exactly like macOS. Pressing an unselected segment tints it with `--p-fill-tertiary` before the selection moves.

## Gray Style

<div style="display: flex; flex-wrap: wrap; gap: 20px; align-items: center; padding: 24px; background: var(--p-bg-secondary); border-radius: 10px; margin: 16px 0;">
  <div class="p-segmented p-segmented-gray" role="group" aria-label="Density">
    <label class="p-segment"><input type="radio" name="sc-density" checked /><span>Compact</span></label>
    <label class="p-segment"><input type="radio" name="sc-density" /><span>Cozy</span></label>
    <label class="p-segment"><input type="radio" name="sc-density" /><span>Roomy</span></label>
  </div>
</div>

```html
<div class="p-segmented p-segmented-gray" role="group" aria-label="Density">
  ...
</div>
```

`.p-segmented-gray` swaps the accent pill for a quiet `--p-fill-tertiary` fill and keeps the label color. Consider it for secondary controls that should not compete with the view's primary action.

## Changing the Color

<div style="display: flex; flex-wrap: wrap; gap: 20px; align-items: center; padding: 24px; background: var(--p-bg-secondary); border-radius: 10px; margin: 16px 0;">
  <div class="p-segmented p-sys-purple" style="background: var(--p-fill-secondary);" role="group" aria-label="Purple example">
    <label class="p-segment"><input type="radio" name="sc-tint1" checked /><span>One</span></label>
    <label class="p-segment"><input type="radio" name="sc-tint1" /><span>Two</span></label>
  </div>
  <div class="p-segmented p-sys-green" style="background: var(--p-fill-secondary);" role="group" aria-label="Green example">
    <label class="p-segment"><input type="radio" name="sc-tint2" checked /><span>One</span></label>
    <label class="p-segment"><input type="radio" name="sc-tint2" /><span>Two</span></label>
  </div>
</div>

Add a `.p-sys-*` class to retint the selected pill through `--p-color-current`, the same mechanism as buttons and form controls. The color class paints its swatch background on the track, so restate the track fill with an inline style or a utility class when you tint.

## States

- **Pressed** on the selected pill darkens it by 13%, the same plus-darker recipe as buttons. Pressed on an unselected segment shows a quiet fill.
- **Focus** shows the forms blue double ring on the focused segment. Arrow keys move the selection, because it is a radio group.
- **Disabled**: put `disabled` on every input. The control fades to 50%, unselected labels drop to `#BFBFBF`, the selected label to half-opacity white.

<div style="display: flex; flex-wrap: wrap; gap: 20px; align-items: center; padding: 24px; background: var(--p-bg-secondary); border-radius: 10px; margin: 16px 0;">
  <div class="p-segmented" role="group" aria-label="Disabled example">
    <label class="p-segment"><input type="radio" name="sc-dis" checked disabled /><span>Label</span></label>
    <label class="p-segment"><input type="radio" name="sc-dis" disabled /><span>Label</span></label>
    <label class="p-segment"><input type="radio" name="sc-dis" disabled /><span>Label</span></label>
  </div>
</div>

## Dark Mode

The track and gray pill flip through the fill tokens, separators lighten to `rgba(255,255,255,0.15)`, and the accent pill picks up the dark accent from `colors.css`. No extra setup.

## The Slide

macOS slides the pill between segments, and so does this control, still without JavaScript. The track watches its own radios through `:has()`, derives the selected index and the segment count, and slides a single pill to `index / count` of its width over 0.25s. Segments are equal-width grid columns, so the position math is exact at any control width. Browsers without `:has()` fall back to a 0.15s crossfade between per-segment pills; the markup is identical either way.

## Migration from v1

| v1 | v2 |
|---|---|
| `.p-segmented-controls` + `.active` | `.p-segmented` with radio inputs |
| `.p-segmented-controls-alt` | `.p-segmented-gray` (closest) |
| `.p-segmented-grey` | `.p-segmented-gray` |
| `.p-segmented-radius`, `-outline`, `-outline-alt` | no v2 equivalent, macOS 26 has one shape |

The v1 classes stay for backwards compatibility and will be removed in a future major. The v2 control needs no `.active` juggling in JavaScript; the checked radio is the state.
